[project]
name = "spark"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dash>=4.2.0",
    "dash-cytoscape>=1.0.2",
    "graphframes>=0.6",
    "numpy>=2.4.6",
    "pyspark>=4.1.1",
]
